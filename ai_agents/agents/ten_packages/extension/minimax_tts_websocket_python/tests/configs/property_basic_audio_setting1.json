{
    "dump": true,
    "dump_path": "./tests/keep_dump_output/",
    "params": {
        "key": "${env:MINIMAX_TTS_KEY}",
        "group_id": "${env:MINIMAX_TTS_GROUPID}",
        "model": "speech-01-turbo",
        "audio_setting": {
            "format": "pcm",
            "sample_rate": 16000
        },
        "voice_setting": {
            "voice_id": "female-shaonv"
        }
    }
}